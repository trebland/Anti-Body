using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackGroundScript : MonoBehaviour
{
    /*
        public Texture Texture2;
        public Texture Texture1;

        void Update () {
            if (MenuScript.instance().playerHealth < 4)
                GetComponent<Texture>(). = Texture2;
            if (MenuScript.instance().playerHealth < 2)
                GetComponent<Texture>() = Texture1;
        }
    */
}